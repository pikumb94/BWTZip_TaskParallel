#pragma once
#include <string>

using namespace std;

string* BWT(string* in) { return in; }

string* MTF(string* in) { return in; }

string* ZLE(string* in) { return in; }

string* ARI(string* in) { return in; }

//why not by reference (instead of by pointers?) how to convert a reference to a pointer?