#pragma once
#include <vector>

using namespace std;

vector <unsigned char>* BWT(vector <unsigned char>* in) { return in; }

vector <unsigned char>* MTF(vector <unsigned char>* in) { return in; }

vector <unsigned char>* ZLE(vector <unsigned char>* in) { return in; }

vector <unsigned char>* ARI(vector <unsigned char>* in) { return in; }

//why not by reference (instead of by pointers?) how to convert a reference to a pointer?